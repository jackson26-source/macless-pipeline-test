{
  "name": "macless-pipeline-test",
  "version": "1.0.0",
  "private": true,
  "description": "Throwaway Capacitor app that exists only to exercise the Macless Android pipeline end to end.",
  "dependencies": {
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
